package src.peer;

public class Client {
    // client socket here, peer info (eg. Peer object, peer id, etc.)
    Client() {
        // start peer process(es) ?
        // handle client socket
    }

    private void readConfig() {
        // read peer info config file
    }
}
